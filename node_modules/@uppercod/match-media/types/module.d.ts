export interface Result {
    value: string;
    size: number;
    unit: string;
    param: string;
    default?: boolean;
}
export default function media(parts: TemplateStringsArray, ...params: any[]): {
    result: Result[];
    match(callback: (result: Result) => boolean): any;
};
export declare function parse(template: string): Result[];
